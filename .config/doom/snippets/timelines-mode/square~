# -*- mode: snippet -*-
# name: square
# key: square
# --
synth "$1_square" $ do
  "amp" <>< let
    in ${2:0}
  "freq" <>< let
    in ${3:f}
  "width" <>< let
    in ${5:0.5}
  "pan" <>< let
    in ${0:0}