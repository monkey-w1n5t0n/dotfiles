# -*- mode: snippet -*-
# name: saw
# key: saw
# --
synth "$1_saw" $ do
  "amp" <>< let
    in vol $ ${2:0}
  ("freq", midSR) <><< let
    in ${3:f}
  ("width", lowSR) <><< let
    in ${5:0.5}
  ("pan", lowSR) <><< let
    in ${0:0}