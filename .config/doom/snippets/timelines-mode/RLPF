# -*- mode: snippet -*-
# name: RLPF
# key: RLPF
# --
synth "${1:name}_RLPF" $ do
  "amp" <>< let
    in vol $ ${2:0}
  "freq" <>< let
    in ${3:2000}
  "rq" <>< let
    in ${4:0.5}
  "pan" <>< let
    in ${0:0}